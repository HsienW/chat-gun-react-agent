{
  "name": "chat-gun-react-agent-bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "HsienW",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run build && node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/errors.test.js dist/server.test.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "~5.7.2"
  }
}
